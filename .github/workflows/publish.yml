# .github/workflows/publish.yml
name: Günlük Deprem Verileri Güncellemesi

on:
  schedule:
    - cron: "*/3 * * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Repo'yu klonla (main branch)
        uses: actions/checkout@v3
        with:
          ref: main

      - name: Node.js kurulumu
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Gerekli paketleri yükle
        run: npm install

      - name: AFAD verisini al
        run: node scripts/afad.js

      - name: Boğaziçi verisini al
        run: node scripts/bogazici.js

      - name: USGS verisini al
        run: node scripts/usgs.js

      - name: /api klasörünü oluştur
        run: mkdir -p api

      - name: JSON dosyalarını taşı
        run: |
          mv afad-depremler.json api/afad-depremler.json
          mv boun-depremler.json api/boun-depremler.json
          mv usgs-depremler.json api/usgs-depremler.json

      - name: Commit ve Push
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add api/*.json
          git commit -m "🔄 JSON güncellendi: $(date '+%Y-%m-%d %H:%M:%S')" || echo "No changes to commit"
          git push
